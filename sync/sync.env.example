# Copy this file to sync.env (same folder) and fill in real values.
# sync.env is gitignored — it should never be committed or shared.

CL_IDENTIFIER=your-login-username-or-email
CL_PASSWORD=your-login-password
